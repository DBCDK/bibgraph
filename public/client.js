// Generated by CoffeeScript 1.6.2
(function() {
  var $graph, klynger, requestKlynge, reset, search, start, update;

  klynger = [];

  reset = function() {
    return void 0;
  };

  start = function() {
    return void 0;
  };

  requestKlynge = function(klynge, done) {
    return $.get("klynge/" + klynge, function(klynge) {
      klynger.push(klynge);
      return $.get("faust/" + klynge.faust[0], function(faust) {
        klynge.title = faust.title;
        update();
        return typeof done === "function" ? done() : void 0;
      });
    });
  };

  $graph = void 0;

  $(function() {
    return $graph = $("#graph");
  });

  update = function() {
    var klynge, _i, _len, _results;

    $graph.empty();
    klynger = klynger.filter(function(klynge) {
      return klynge.adhl;
    });
    _results = [];
    for (_i = 0, _len = klynger.length; _i < _len; _i++) {
      klynge = klynger[_i];
      _results.push($graph.append("<div>" + klynge.title + " " + klynge.adhl[0][1] + "</div>"));
    }
    return _results;
  };

  search = function() {
    var query;

    reset();
    query = ($("#query")).css({
      display: "none"
    }).val();
    location.hash = query;
    klynger = [];
    qp.log("search", query);
    return $.get("search/" + query, function(result) {
      ($("#query")).css({
        display: "inline"
      }).val("");
      return async.map(result, function(faust, done) {
        return $.get("faust/" + faust, function(faust) {
          if (faust != null ? faust.klynge : void 0) {
            return requestKlynge(faust != null ? faust.klynge : void 0, done);
          } else {
            return done();
          }
        });
      }, function() {
        var klynge, max, _i, _len;

        max = {
          adhl: [[0, 0]]
        };
        for (_i = 0, _len = klynger.length; _i < _len; _i++) {
          klynge = klynger[_i];
          if (max.adhl[0][1] <= klynge.adhl[0][1]) {
            max = klynge;
          }
        }
        klynger = [max];
        return update();
      });
    });
  };

  $(function() {
    ($("#search")).on("submit", function() {
      search();
      return false;
    });
    if (location.hash) {
      ($("#query")).val(location.hash.slice(1));
      return search();
    }
  });

}).call(this);
